spring:
  application:
    name: cloudpool-compute
    version: 0.1.0

  # Datasource Configuration
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:h2:file:../data/cloudpooldb;AUTO_SERVER=TRUE;DB_CLOSE_DELAY=-1;MODE=PostgreSQL}
    username: ${SPRING_DATASOURCE_USERNAME:sa}
    password: ${SPRING_DATASOURCE_PASSWORD:password}
    driver-class-name: org.h2.Driver
    hikari:
      maximum-pool-size: 50
      minimum-idle: 10
      idle-timeout: 300000
      connection-timeout: 20000
      max-lifetime: 1200000

  # Server Configuration
  server:
    port: 8084
    servlet:
      context-path: /

  # JPA/Hibernate Configuration
  jpa:
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true
        jdbc:
          batch_size: 20
          fetch_size: 50
        order_inserts: true
        order_updates: true
        generate_statistics: false

  # Jackson Configuration
  jackson:
    serialization:
      write-dates-as-timestamps: false
      indent-output: true
    deserialization:
      fail-on-unknown-properties: false
    default-property-inclusion: non_null

# Logging Configuration
logging:
  level:
    root: INFO
    com.cloudpool: DEBUG
    org.springframework.web: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [traceId=%X{traceId} spanId=%X{spanId}] - %logger{36} - %msg%n"
    file: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level [traceId=%X{traceId} spanId=%X{spanId}] %logger{36} - %msg%n"
  file:
    name: logs/cloudpool-compute.log
    max-size: 10MB
    max-history: 30

# Actuator Configuration
management:
  endpoints:
    web:
      exposure:
        include: health,metrics,prometheus,info
      base-path: /actuator
  endpoint:
    health:
      show-details: always
  metrics:
    export:
      prometheus:
        enabled: true

# Custom Application Properties
cloudpool:
  jwt:
    secret: ${JWT_SECRET}
    expiration-ms: ${JWT_EXPIRATION_MS:3600000}
  
  cors:
    allowed-origins: ${CLOUDPOOL_CORS_ALLOWED_ORIGINS:http://localhost:3000,http://localhost:8080,http://cloudpool.empirebot.in,http://*.empirebot.in}
 
  encryption:
    master-key: ${CLOUDPOOL_ENCRYPTION_MASTER_KEY}
 
  rate-limit:
    requests-per-minute: ${CLOUDPOOL_RATE_LIMIT_RPM:120}

  email:
    smtp-host: ${CLOUDPOOL_EMAIL_SMTP_HOST:smtp.cloudpool-email.com}
    smtp-port: ${CLOUDPOOL_EMAIL_SMTP_PORT:587}
    smtp-username: ${CLOUDPOOL_EMAIL_SMTP_USER:noreply@cloudpool-email.com}
    smtp-password: ${CLOUDPOOL_EMAIL_SMTP_PASSWORD:securepassword}
    smtp-from: ${CLOUDPOOL_EMAIL_SMTP_FROM:noreply@cloudpool-email.com}
    sandbox-mode: ${CLOUDPOOL_EMAIL_SANDBOX:true}
    embedded-smtp:
      enabled: ${CLOUDPOOL_EMBEDDED_SMTP_ENABLED:true}
      port: ${CLOUDPOOL_EMBEDDED_SMTP_PORT:2525}

server:
  port: 8084

server:
  forward-headers-strategy: FRAMEWORK

