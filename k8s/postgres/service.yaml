apiVersion: v1
kind: Service
metadata:
  name: postgres-headless
  namespace: default
  labels:
    app: cloudpool-postgres
spec:
  ports:
  - port: 5432
    name: postgres
  clusterIP: None
  selector:
    app: cloudpool-postgres
---
apiVersion: v1
kind: Service
metadata:
  name: cloudpool-postgres
  namespace: default
  labels:
    app: cloudpool-postgres
spec:
  ports:
  - port: 5432
    name: postgres
  selector:
    app: cloudpool-postgres
